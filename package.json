{
  "name": "chrislross.com",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "node app.js",
    "build": "pug --out dist --pretty views/index.pug && copyfiles -f ./public/styles/* ./dist/styles/ && copyfiles -f ./public/favicon.ico ./dist/ && copyfiles -f ./public/images/* ./dist/images/ && copyfiles -f ./public/documents/* ./dist/documents/"
  },
  "description": "chrislross.com",
  "author": {
    "name": "https://github.com/fightingsleep"
  },
  "dependencies": {
    "express": "^4.18.2",
    "pug": "^3.0.2"
  },
  "devDependencies": {
    "copyfiles": "^2.4.1",
    "pug-cli": "^1.0.0-alpha6"
  }
}
